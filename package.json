{
  "name": "bankim-management-portal",
  "version": "1.0.0",
  "private": true,
  "description": "BankIM Management Portal - Monorepo with client, server, and shared packages",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check",
    "deploy": "npm run build && npm run type-check && git push origin main",
    "deploy:force": "npm run build && git push origin main",
    "deploy:test": "npm run build && npm run type-check && npm test --workspace=@bankim/client && git push origin main",
    "validate": "npm run type-check && npm run build",
    "start": "turbo run dev --parallel"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "playwright": "^1.54.2",
    "turbo": "^1.11.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "author": "BankIM Development Team",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.11.0",
    "dotenv": "^17.2.1",
    "pg": "^8.16.3"
  }
}
