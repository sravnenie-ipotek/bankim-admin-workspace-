{
  "name": "@bankim/shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for BankIM Management Portal",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json"
  },
  "files": [
    "dist/**/*"
  ],
  "keywords": ["bankim", "shared", "types"],
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}