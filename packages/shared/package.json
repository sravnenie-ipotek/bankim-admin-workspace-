{
  "name": "@bankim/shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for BankIM Management Portal",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "dev": "npx tsc -w -p tsconfig.json",
    "test": "echo 'No tests yet'",
    "lint": "echo 'No linting yet'",
    "type-check": "npx tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "files": [
    "dist/**/*"
  ],
  "keywords": ["bankim", "shared", "types"],
  "license": "MIT",
  "private": false,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sravnenie-ipotek/bankim-admin-shared.git"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}